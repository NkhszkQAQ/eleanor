# 首次本地验收记录（发布前）

以下为首次本地制作时的验收快照，保留当时的验证范围；不是当前 GitHub Pages 部署状态记录。

验收日期：2026-09-11。浏览器：本机 Chrome 153.0.8010.37，由 Playwright 驱动独立无头实例。使用本地静态 HTTP 服务与 `file://` 两种加载方式，无远端发布。

## 实际通过

| 检查 | 结果 |
| --- | --- |
| 375px、768px、1440px 页面 | 缺失素材和有效素材两种状态均通过 DOM 尺寸检查与截图目视核对；页面宽度分别为 375、768、1440，无横向溢出或控件遮挡 |
| 照片比例 | 使用横图、竖图、方图和极宽／极高几何测试图；缩略图与大图均 `object-fit: contain`，截图中的圆保持圆形，完整图片不被拉伸或裁切 |
| 初始状态 | Hero 隐藏；无姓名、祝福、经历或照片说明；两个音频均暂停、时间未推进、未设置 autoplay |
| 音频播放 | 有效 MP3 的实际时长约为 5 秒和 6 秒；两段各自验证播放、暂停、暂停后时间不变、鼠标进度调整、滑块 Home／End／方向键调整 |
| 音频互斥 | 双向切换以及 10 次快速交替点击，未出现两段同时播放 |
| 播放结束 | 两段均实际触发 ended；按钮恢复播放状态；点击可从头重播 |
| 缺失音频 | HTTP 与直接打开 HTML 均显示“音频暂不可用”；未知时长 `--:--`，进度条禁用；重试后仍正确报告错误 |
| 损坏音频 | 以刻意损坏的临时测试文件触发浏览器解码错误；换回有效 MP3 后点击重试，无需刷新即可播放 |
| 浏览器拒绝播放 | 额外以 API 错误注入模拟 NotAllowedError；显示可恢复提示，恢复 API 后可再次播放。此项为模拟错误，不是实际浏览器权限拦截 |
| 照片墙 | 6 张真实可解码测试 JPG 均加载；缺失照片显示占位；图片使用原生 loading="lazy" |
| 查看器 | 点击、Enter 打开；前后按钮与左右键循环切换；Esc、关闭按钮、遮罩关闭；缺失图片仍可切换 |
| 焦点与滚动 | 打开后焦点移至关闭按钮；Tab／Shift+Tab 限制在查看器；背景 body 固定；关闭事件后原滚动位置和原照片按钮焦点恢复 |
| 查看器布局 | 375px、768px、1440px 截图核对；大图完整显示，关闭和前后按钮可见 |
| 减少动态效果 | 模拟 prefers-reduced-motion: reduce，按钮过渡为 0 秒，查看器仍可打开、切换、关闭 |
| 触屏 | 375px Chromium 触屏模拟中，点击播放／暂停及图片前后／关闭均通过 |
| 配置边界 | 空照片数组、单张、增加至 7 张；Hero 仅标题／仅正文；自定义歌名、alt、caption；长连续文本在 375px 下无横向溢出 |
| 文本与焦点 | 说明按纯文本显示，不解析 HTML；200% 字号下 375px 无横向溢出；键盘焦点有可见轮廓 |
| 仓库子路径 | 从 `/repo/` 加载页面；CSS、两个 JS、两个 MP3 和六张 JPG 均从 `/repo/…` 成功返回，无根路径依赖 |
| 直接打开 | `file://` 下配置、CSS、有效音频播放和图片查看器通过；交付项目缺失素材提示也通过 |
| 脚本 | 两个 JS 文件通过 node --check；上述浏览器检查无未捕获 JavaScript 异常 |
| 部署包 | ZIP 根目录直接包含 index.html；包内文件与项目一致；没有测试音频、测试图片、依赖、临时路径或个人素材 |

## 测试材料与证据

MP3 是临时生成的低音量纯音；JPG 是标注 TEST FIXTURE 的几何色块。它们只存在于临时验收副本，未放入此项目或 ZIP。项目默认保持真实素材待替换状态。

项目目录外的 `outputs/verification/` 留有验收脚本、结构化结果与截图。`valid-*.png` 和 `viewer-*.png` 中的图形仅为验收用图，不是网站照片内容；`missing-*.png` 为交付默认状态。

## 未验证的范围

- 未创建 GitHub 仓库、推送代码或发布 GitHub Pages；“可部署”指包结构和本地子路径验证通过，不表示已上线。
- 未做 Safari／Firefox 浏览器执行测试、实体手机测试、屏幕阅读器实测或扬声器听感验收。
- 用户最终提供的音频、照片和文案尚未加入，替换后仍需本地预览并在发布后检查。
- 减少透明度和增强对比度有 CSS 适配，未单独做浏览器偏好模拟验收。
