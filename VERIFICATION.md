# 礼物网站重设计验收

日期：2026-09-11。环境：本机 Chrome 153，由 Playwright 驱动独立无头浏览器。本记录对应三页重设计，不是线上 GitHub Pages 发布记录。

## 图片继续加速至少 30%（当前增量）

基线：main a8b56f821f5e2fbc417c9f117c8d6ac430202528（已包含上一轮 WebP 加速和默认音乐播放）。新增 480／最大 960px AVIF 自适应预览，当前可见照片高优先级，仅在视口附近 160px 设置图片源；不支持 IntersectionObserver 时回退原生懒加载。AVIF 不支持或失败 → WebP → 原图逐级回退。查看器先显示已有预览，再由完整原图替换；原图失败保留预览并提示，不伪装为原图加载成功。

本机 Chrome、100ms 延迟、200,000 B/s 下载、冷缓存，优化前后交错五轮，音乐在两版均真实播放。统计每个宽度初始视口内所有照片（含部分可见的下一行），从导航开始至图片 load 之后两个动画帧的最晚完成时间；中位数如下：

| 视口（高度 900px） | 像素密度 | 可见照片数 | 基线 | 本轮 | 耗时减少 |
| --- | --- | --- | --- | --- | --- |
| 375px | 3 | 2 | 2536.3ms | 1508.7ms | 40.5% |
| 768px | 2 | 4 | 4708.0ms | 2315.3ms | 50.8% |
| 1440px | 1 | 6 | 6500.5ms | 1436.7ms | 77.9% |

这是相对上一轮加速版的进一步改善，不是与最初原图版比较。仅证明上述本地限速条件下的首屏显示耗时；不代表任意网络、任何单张图或全尺寸原图下载都快 30%。没有减少原图的字节量。原始每次数据与脚本位于项目外 outputs/verification/redesign/photo30-benchmark.json 和 photo30-benchmark.cjs。

功能实测：三个宽度逐张解码全部 18 张 AVIF，比例正确，375px 高像素密度和桌面截图已查看；200% 字号无横向溢出。照片只在视口附近请求，正常首屏没有原图／WebP 重复下载；延迟第 15 张原图时预览可见，随后被 3024px 宽原图替换。方向键、Esc、关闭、焦点和滚动恢复、减少动态效果、file://、AVIF 请求失败、WebP 请求失败、全部失败、模拟不支持 AVIF／IntersectionObserver、原图失败后保留预览均通过；无未捕获脚本错误。18 张原图 SHA-256 与 originals.json 一致。结果见项目外 photo30-functional-results.json。未做实体手机、Safari／微信或真实地区网络测速。

以下为历史记录。

## 每次打开默认播放（当前增量）

新增 audio.song.autoplay，默认 true；三页初始化直接尝试播放，保持 20% 增益，不再由拆包动画静音或重置歌曲。旧的主动拆包播放仅在 autoplay 关闭时生效。浏览器拒绝时保留真实提示，首次可信点击或 Enter／空格操作重试；播放器控件自行处理手势，暂停后不被全局点击重新启动。pagehide 移除重试监听。

Chrome 分别使用允许自动播放和要求用户手势两种策略，首页、相册、语音页均通过：允许时实际播放且时间递增；拒绝时实际暂停并显示提示，点击后播放；手动暂停后后续点击不恢复、手动继续与跳转正常；允许策略下刷新重新播放。375×812 浏览器环境，无未捕获脚本错误。结果在项目外 outputs/verification/redesign/autoplay-results.json。未做实体手机或 Safari／微信验收，不能保证浏览器一定允许有声自动播放。以下关于手势后才请求音乐及拆包期间静音的描述为历史行为。

## 媒体加速（当前增量）

2026-09-11：基线为 main 107b63b；18 张 JPG 和原始 MP3 的 SHA-256／字节比较不变。新增 WebP 预览总量 1,538,018 字节，比原图总量少 87.6%；96 kbps MP3 播放副本为 2,185,552 字节，比原音乐少 40.0%。这些是文件体积比较，不是网速承诺。

Chrome 冷缓存、1440×900、延迟 100ms、下载 200,000 字节/秒，优化前后交错执行。照片首屏前三张从导航开始到响应完成（随后确认 is-loaded）五次中位数 4,815.4 → 2,790.1ms，耗时减少 42.1%；音乐从播放点击到缓冲可覆盖前 30 秒，三次中位数 3,121 → 1,921ms，耗时减少 38.4%。音乐首次 playing 事件五次中位数 282.2 → 285.3ms，没有改善，因此不宣称起播快 15%。本地 HTTP 服务、独立 Chrome 测量；未测 GitHub 边缘节点与实体手机网络，不代表所有用户都能获得同样提升。

功能回归：375/768/1440px、200% 字号、照片比例、18 张懒加载、方向键/关闭/焦点恢复、减少动态效果、file://、图片缺失均通过。照片墙只请求预览，点击第 15 张下载 3024px 宽的原图；单独阻断预览时回退原图通过。已查看手机预览截图。音频实际解码、有效时长、暂停/跳转/继续、20% 增益、拆包期间静音、语音互斥、缺失/被阻止播放、无自动重播、file:// 原生回退通过；没有未捕获脚本错误。未做实体手机、Safari/Firefox 或主观音质验收。原图不压缩，预览及音乐播放副本为有损编码。

完整原始记录和测试脚本保存在项目外 outputs/verification/redesign/media-speed-*。以下是历史记录，当前照片墙使用预览，查看器才加载原图。

## 18 张原图与图钉照片墙（当前增量）

2026-09-11：18 张 JPG 与用户文件逐字节及 SHA-256 核对一致，共 12,444,115 字节；指定 PNG 截图未加入。保留原始编码和 EXIF，配置显示方向的宽高；照片与查看器均使用同一原文件。文案仍留空。

本机 Chrome 实际通过：375／768／1440px 无横向溢出，200% 字号可用；18 张图片均解码成功，EXIF 方向与配置尺寸一致、完整比例显示；首次请求少于 18 张，滚动后逐张加载。已查看手机和桌面截图。触屏与键盘打开、左右切换、Esc／关闭按钮／遮罩关闭、焦点和滚动恢复通过。减少动态效果取消倾斜，键盘仍可用；缺失图片占位与查看器错误提示、仓库子路径及 file:// 加载通过，无未捕获脚本错误。

原始记录：项目外 outputs/verification/redesign/pinned-photos-results.json。本次是本地浏览器验证，未做实体手机、Safari／Firefox 验收，尚不代表合并或线上部署。音乐代码未改动，其验证见以下历史记录。

## 音乐信息与状态补全（历史增量）

2026-09-11：从用户提供的文件名填写歌名「天使にふれたよ!」及署名「椎名モンブラン」。ffprobe 确认 ID3 没有标题／演唱者字段，署名仅来自文件名；测得时长 182.07345 秒。配置中保存与音频路径对应的实测时长，尚未请求音频时显示 0:00 / 3:02；浏览器实际元数据优先，进度控件仍在取得有效时长后启用。补齐点击播放、正在播放、已暂停状态，保留错误／缓冲／结束提示。

实际检查：三个页面 × 375／768／1440px，标题、署名、时间和状态均显示，初始音乐请求数为零；200% 字号无横向溢出。修正了测试发现的语音页底部固定宽度音乐栏在 768px／200% 字号下溢出。真实音乐播放、暂停、跳转、继续、结束状态通过；错误文件清除时长预览，变更音频路径忽略旧预存数据，浏览器时长覆盖不一致的预览值。拆包期间静音／结束后 20% 增益、语音互斥、键盘／减少动态效果、缺失／被阻止播放、pagehide 及 file:// 回归通过；无未捕获脚本错误。

记录：项目外 `outputs/verification/redesign/music-track-details-results.json` 及更新后的 `background-music-results.json`。未做实体手机或 Safari／Firefox 测试。本节为本地验证，不代表已合并或线上验收；以下是历史记录。

## 拆包后轻声播放背景音乐（历史增量）

2026-09-11：接入用户提供的真实 MP3（3,645,565 字节，复制后 SHA-256 与原文件一致，未转码）。独立文件由 Pages 托管，不嵌入 HTML／JS。音频均设置 preload=none，只有主动拆包／播放音乐或打开语音信封才开始请求。背景音乐默认增益 0.2；点击拆包时在用户手势内解锁播放，动画期间输出为零，进入首页后从头开始、0.6 秒淡入。保留原有播放器、互斥和首次访问记录。

本次 Chrome 实际验证：真实歌曲解码和有效时长、拆包前零音乐请求、动画期间 GainNode=0、结束后 GainNode=0.2 且分析器检测到实际非零输出；暂停时间不变、进度调整与继续播放；375／768／1440px 无溢出，刷新不自行恢复音乐。键盘及减少动态效果正常启动，未拆包直接跳过／pagehide 不留后台播放；强制播放拒绝和缺失文件提供真实状态。与临时有效语音录音互斥，file:// 与无 Web Audio 原生音量回退通过；无未捕获脚本错误。JS 语法和最终差异检查通过。

测试记录在项目外 `outputs/verification/redesign/background-music-results.json`。测试语音未加入交付包；新增 song.mp3 是用户明确提供的正式素材。未执行实体 iPhone／Android、Safari／Firefox、扬声器听感测试；原生音量回退可能受设备限制，浏览器拦截时仍需手动点击播放。以下为历史验证记录。

## 清晰度与完整包裹淡出（历史版本）

2026-09-11：保留既有丝带分层、位移方向与时长，移除四片包装纸及对应裁切和飞散样式。丝带移开后，完整包裹短暂停留，开场在 650ms 后整体淡出，于约 1.15 秒关闭并恢复首页焦点。换用细节增强后的无损 WebP，实际分辨率仍为 1536×1024，两张共 1,830,592 字节；编码前后 RGB 数据逐字节一致。未修改首次访问存储键或文案。

实际验证：320／375／768／1440px 和 812×375 横屏；手机三倍像素密度、桌面两倍像素密度，检查初始及过渡截图，无横向溢出；丝带仍产生位移，包装纸无碎片节点、无位移且保持完整。结束后焦点与滚动恢复、音频不自动播放；刷新不开场且不再请求图片。键盘与减少动态效果直接进入；缺失主图、缺失底图、慢加载期间跳过可用；file://、375px 下 200% 字号及语音页往返通过。没有采集到未捕获脚本异常。语音播放器与相册代码未改动。

原始记录：项目外 `outputs/verification/redesign/sharp-parcel-results.json`。未做实体手机、Safari 或 Firefox 验收。以下均为历史版本记录。

## 已确认的酒红细丝带增量验收（历史版本）

2026-09-11：用用户确认的生成图替换旧版图标，保持细丝带、小结、长尾和深酒红色；新增对齐的无丝带包装纸图层，通过 SVG 分层与 CSS 位移、淡出呈现拆开过程。两张图均为本地相对路径，仅首次开场请求，合计 305,627 字节。

本次实际执行：320／375／768／1440px 和 812×375 横屏的触屏模拟，无文档或开场横向溢出；检查初始、丝带移开、包装纸展开截图。完成后焦点回到首页、滚动恢复、无自动播放；刷新不再次开场，也无新增图案请求。键盘与减少动态效果直接进入；主图缺失不阻塞首页、第二图缺失可简化进入、慢加载期间跳过后不会重新弹出。file://、375px 下 200% 字号、语音页往返均通过，采集范围内无未捕获脚本异常。开场及首页标题正文仍为空，原有持久存储键未改变。

原始记录为项目外 `outputs/verification/redesign/approved-parcel-results.json`。本次未修改播放器或相册实现，以下媒体测试为先前版本的实际记录。本地通过不代表线上或实体手机已验收；本轮未执行实体手机和 Safari／Firefox 测试。

以下是历史版本记录；旧版图标已移除，当前素材说明以 README 和 assets/artwork/README.md 为准。

## 开源 SVG 蝴蝶结增量验收

2026-09-11：搜索并比较 CSS 与开源 SVG 实现后，使用 Microsoft Fluent UI Emoji 的 Ribbon 图形替换自行拼接的蝴蝶结。调整为网站暖粉色，保留原始曲线及渐变层次，并分别处理结心、左右结环和尾带的解开过渡。随包保留 MIT 许可证和来源说明。

已实际验证 320／375／768／1440px 和手机横屏，查看静止及解开中间帧；渐变与滤镜引用均正确，无横向溢出，无外部资源请求。触屏解开后焦点和首页恢复，刷新不再展示；文案仍留白、无重播入口、不自动播放音乐。键盘与减少动态效果即时进入，file:// 与 375px 的 200% 字号可用，无脚本异常。原始记录：项目外 `outputs/verification/redesign/svg-bow-results.json`。本轮未改动首次访问逻辑；实体手机尚未验收。

## 包装纸与首次访问修正

2026-09-11：按用户确认改为柔和包装纸和丝带结，清空开场及首页标题、正文，保留两处空白文案区域；移除重播入口。使用 localStorage 保存首次展示记录，并兼容旧 sessionStorage 记录。

本次实际验证：320／375／768／1440px 与手机横屏下的丝带展开中间帧、完成和空白区域；同浏览器刷新、新标签、子页返回均不重播；用真实持久浏览器配置目录关闭并重启 Chrome 后仍不重播；旧会话标记迁移、减少动态效果、键盘进入、禁用存储时可继续访问、375px 的 200% 字号无溢出。无脚本异常。结果为项目外 `outputs/verification/redesign/parcel-refinement-results.json`。

以下开场增量记录是上一版历史测试，关于“按会话展示”和“重播入口”的描述已由本节替代。实体手机仍未验收。

## 拆包裹开场增量验收

2026-09-11：新增首页首次会话开场及重播，沿用设计守卫的触屏、键盘和减少动态效果规则。以下为本次新增功能的实际验证：

- Chrome 触屏模拟：320×568、375×812、768×1024、812×375 横屏；桌面 1440×900。轻触／点击后包装纸真实产生过渡，完成后进入首页，无横向溢出；初始与展开中途均有截图。
- 完成后恢复背景滚动与首页焦点；跳过、Esc、Tab／Shift+Tab 焦点约束、重播，以及动画中途跳过后立即重播均通过。清除的旧定时器不会误关新开场。
- 同一会话刷新、进入语音／相册后返回首页均不重复。开场结束不自动播放音频，既有真实测试 MP3 仍能手动播放。
- 键盘拆包直接进入；减少动态效果及动画中途开启该设置均即时完成。
- 375px、200% 字号无横向溢出且可点击跳过；禁止会话存储仍可正常进入；配置关闭开场、直接 file:// 打开、禁用 JavaScript 时无开场遮挡均通过。
- 无未捕获 JavaScript 异常。原始结果为项目外的 `outputs/verification/redesign/parcel-results.json`。手机结果为浏览器触屏模拟，尚未在实体 iPhone／Android 上验收。

## 已实际验证

| 范围 | 证据与结果 |
| --- | --- |
| 三个页面 | `index.html` 为统一入口与音乐；`voices.html` 是独立语音页，默认九封信封；`photos.html` 为独立相册。首页不内嵌相册或九封信封，两页均可返回首页 |
| 响应式 | 三页在 375、768、1440px，分别以缺失素材和有效测试素材渲染并截图；文档宽度等于视口宽度，无横向溢出。抽查截图未见遮挡或拉伸 |
| 文字放大 | 三页在 375px、200% 字号下无横向溢出；修复了实际时长文本在紧凑音乐布局下的溢出 |
| 九份录音 | 九个不同路径逐一解码、播放、暂停、调整进度；默认不自动播放，打开信封才加载对应录音 |
| 音频控制 | 鼠标拖动、Home／End／左右键调整实际进度；暂停时间不变；播放结束和从头重播通过 |
| 互斥 | 音乐可继续播放到用户主动播放录音；录音开始后音乐暂停；切换信封、关闭信纸会暂停旧录音，下一份不自动播放 |
| 页面离开 | 链接跳转和浏览器返回后不会残留播放中的录音、固定背景或打开的信纸 |
| 信纸查看器 | Enter 打开、前后循环切换、Tab／Shift+Tab 焦点约束、关闭按钮／Esc／遮罩关闭、焦点与滚动恢复通过；滑块上的方向键不会误切换信封 |
| 照片查看器 | 有效图片显示、前后循环、Esc、原按钮焦点恢复通过；图片使用 contain 保持完整比例 |
| 大量照片 | 137 张配置，首批 24 张，滚动追加成功；禁用 IntersectionObserver 后，通过“加载更多照片”到达全部 137 张，焦点进入首张新增照片，末尾按钮隐藏 |
| 跨批查看 | 查看器可从第 24 张切换到尚未渲染缩略图的第 25 张，完整列表不受当前批次限制 |
| 触屏 | 375px Chromium 触屏模拟下，信封打开、播放、切换与关闭通过 |
| 减少动态效果 | 模拟 reduced motion，过渡为 0 秒，信封及弹窗操作仍可用；键盘输入不触发动效 |
| 缺失与损坏素材 | 缺失状态显示简洁提示、未知时长和禁用进度；临时损坏录音触发真实解码错误，换回有效文件后重试可恢复 |
| 内容配置 | 空照片列表、空录音列表、单份录音、可选 Hero、真实姓名／寄语字段、长寄语滚动、alt／caption、width／height 比例预留通过；配置文本不解析 HTML |
| 静态与子路径 | `file://` 直接打开第九份录音可播放；HTTP `/repo/` 下三个页面、录音和照片可加载，无构建步骤 |
| 脚本 | `node --check` 通过；上述功能及边界测试无未捕获 JavaScript 异常 |

## 独立入口修订复验

本次将相册从首页移至 `photos.html`，保留已有播放器与查看器逻辑。新增路由测试通过：三页 × 三种宽度 × 有效／缺失素材共 18 张截图；三页 200% 字号无横向溢出；键盘打开两个入口、九封信封与六张照片计数、有效录音播放、相册前后切换及焦点恢复、返回首页、浏览器返回无自动续播、减少动态效果、缺失素材提示、`file://` 完整往返链路。无未捕获脚本异常。原媒体与大量照片测试为前一版本执行结果，播放器和分页实现未改动。

原始复验记录为项目外的 `outputs/verification/redesign/routing-results.json`。

## 设计守卫落实

沿用奶油纸色与暖棕强调色，使用本机宋体呈现纸张式标题，正文保留系统字体。信封、封口与唱片均为静态 CSS／SVG 装饰，无虚构人物或照片。

180ms 的位移反馈只用于精确指针 hover；键盘路径无动画。减少动态效果采用静态反馈，减少透明度与提高对比度有独立 CSS 适配。纸张角度、间距和字号是本项目选择，不声称是技能规定的固定数值。

## 边界与未完成验收

- 测试 MP3 为临时纯音，照片为几何比例测试图；未加入项目、ZIP 或远端仓库。
- 真实的九人录音、姓名和照片尚未提供；歌曲已由用户提供并完成上述增量验证，其余素材替换后仍需验收。
- “无固定总张数上限”是配置与分页逻辑没有上限，不代表设备内存、网络或 GitHub 托管存储无限。
- 未执行实体手机、Safari／Firefox、屏幕阅读器或扬声器听感测试。
- 本轮重设计在任务分支交付；本地验证不等于已合并或已更新线上。
- 原始结果和截图位于项目外 `outputs/verification/redesign/`，不会成为网页资源。
