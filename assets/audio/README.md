背景音乐 song.mp3 已由用户提供，原文件为「天使にふれたよ!    椎名モンブラン~(3).mp3」，复制时未转码或改动音频数据（3,645,565 字节）。
音频作为独立文件托管在 GitHub Pages；HTML 和 JavaScript 仅引用路径，不嵌入音频。默认不预加载，主动拆包或播放时才请求。
在 ../../content.js 的 audio.song 中调整 volume（当前 0.2）和 playAfterOpening（当前 true）。页面仍仅显示“音乐”，没有自行添加文案。
九个人的真实录音放入 voices/01.mp3 至 voices/09.mp3；姓名与寄语在 voices 数组填写。录音和照片尚未提供。
本目录不包含测试录音或伪装成媒体的空文件。请使用真正的 MP3 编码，改扩展名不会转换格式。
